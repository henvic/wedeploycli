clone_folder: c:\gopath\src\github.com\wedeploy\cli
environment:
  GOPATH: c:\gopath
install:
  - echo %PATH%
  - echo %GOPATH%
  - go version
  - go env
  - go get -u github.com/golang/dep/cmd/dep
  - dep ensure
build_script:
  - go test -v $(go test $(go list ./... | grep -v /vendor/) -race)
